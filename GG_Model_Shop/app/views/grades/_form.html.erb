<%= form_for(@grade) do |f| %>
  <% if @grade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grade.errors.count, "error") %> prohibited this grade from being saved:</h2>

      <ul>
      <% @grade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :grade_type %><br />
    <%= f.text_field :grade_type %>
  </div>
  <div class="field">
    <%= f.label :gundam_model_id %><br />
    <%= f.number_field :gundam_model_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
